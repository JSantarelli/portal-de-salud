import { Prestacion } from "./prestacion";

export class Card {
    titulo: string;
    subtitulo: string;
    icono: string;
    botones: [string, string];
    type: string;
    fecha: string;
    path: string;
    outlet: string;
}